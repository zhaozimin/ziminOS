# tests/

> L2 | 父级: ../CLAUDE.md

这里收口可以离开 Obsidian 真机验证的回归行为。测试直接编译 `src/` 里的 TypeScript 事实源，不维护第二份实现；平台结构检查只钉住「移动端加载前不引入 Node」这条不能由纯函数表达的边界。专业版多出的出库单测试按源文件是否存在自动启用，因此同一入口可用于两个版次。

## 成员清单

regression.mjs: Node 内建测试入口，用 esbuild 内存编译事实源；18 项基础回归覆盖版本镜像、Gitee 一键安装入口同构、日期严格性、划线身份与批次归并、行尾保真、ISBN、持久化设置验形、损坏外观配置拒写、书源漏匹配说明、异步命令边界、Apple Books 数据库选择、项目流转事实回滚与移动端 Node 边界，专业版源码存在时再自动追加出库单往返测试。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
