# skill/

> L2 | 父级: ../CLAUDE.md

## 成员清单

SKILL.md: 桌面 Agent 的原地安装/升级契约；把当前工作区锁定为唯一笔记库，只从外部临时施工源交付 vault 成品；全新安装启用 Dataview、默认外观、十二个 CSS 片段（十个默认开）与属性类型登记表 types.json，升级更新锁定运行文件并保护笔记和用户配置——types.json 只补缺失的属性键，用户调过的类型一律不动；enabledCssSnippets 只为本次新增的片段追加，用户在右下角外观开关里关掉的片段，升级不得替他重新打开。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
