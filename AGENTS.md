# Agent 路由

如果用户已经创建并命名一个文件夹、用桌面 Agent 打开它，再给出本仓库地址并要求“按照仓库打造、搭建或安装个人知识管理系统”，立即读取并严格执行 `skill/SKILL.md`。

Agent 当前工作区就是最终 Obsidian 笔记库。不得询问名称或路径，不得创建另一层目录，不得在当前工作区克隆源码仓库。源码只能临时放在工作区外；最终只把仓库 `vault/` 的内部内容铺到当前工作区根目录。不得安装 Node.js、npm 依赖或任何额外插件。

如果任务是开发或维护 ziminOS，再读取 `CLAUDE.md` 与 `docs/设计规格书.md`。
