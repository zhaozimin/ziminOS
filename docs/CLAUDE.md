# docs/

> L2 | 父级: ../CLAUDE.md

## 成员清单

设计规格书.md: V1（v0.3.1，项目管理与灵感收集）的唯一设计事实源；描述已交付形态，是 V2 之前一切行为的判据。
设计规格书-V2.md: V2（v0.4.0，人脉管理与复盘）的唯一设计事实源，此后逐版追加章节；两文件并存，交集处以 V2 为准。它定下三件 V1 没有的事——视图由插件自渲染代码块而非 DataviewJS、复盘时间轴独占第七个根文件夹、主题由命令回填而非建笔记时弹窗。§16 起是逐版追加的产品决定：作者名片（v0.9.0）、检索排除功能目录与名片进 README（v0.10.0）、读书笔记系统（v0.12.0）。§19 那一章还兼一份判例：它把「多插件协作还是自己实现」这个问题连同判据一起记下来了——三家阅读 App 与豆瓣的同步插件全走私有网络接口，因此改走官方纯文本导出、由学员粘贴，红线一条不破。日后再遇到「要不要接某个外部数据源」，照那一节的判据走。
第三方组件.md: Dataview 查询组件、外观包与两套编译进 main.js 的图形（Pikaicons 命令图标、Simple Icons 品牌 logo）的供应链锁定表；记录上游版本、发布资产校验、许可证与用户配置保护边界。它还收着十二个 CSS 片段的清单：每个片段的来源、默认开关，以及落地时对原始文件做过什么处理——两处改动（文件图标前缀按 ziminOS 目录重写、水平滑轮的选择器收窄）在此备案，否则日后无从判断成品与上游为何不一致。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
