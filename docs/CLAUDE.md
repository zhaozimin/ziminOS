# docs/

> L2 | 父级: ../CLAUDE.md

## 成员清单

设计规格书.md: V1（v0.3.1，项目管理与灵感收集）的唯一设计事实源；描述已交付形态，是 V2 之前一切行为的判据。
设计规格书-V2.md: V2（v0.4.0，人脉管理与复盘）的唯一设计事实源；两文件并存，交集处以 V2 为准。它定下三件 V1 没有的事——视图由插件自渲染代码块而非 DataviewJS、复盘时间轴独占第七个根文件夹、主题由命令回填而非建笔记时弹窗。
第三方组件.md: Dataview 查询组件与外观包的供应链锁定表；记录上游版本、发布资产校验、许可证与用户配置保护边界。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
