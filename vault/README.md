
```ziminos
关于作者
```

（ziminOS 启用后，上面会显示一张带链接的名片。插件还没启用时链接在这里：官网 [zhaozimin.cn](https://zhaozimin.cn) / [zhaozimin.com](https://zhaozimin.com)，插件教程 [edu.zhaozimin.cn](https://edu.zhaozimin.cn) / [edu.zhaozimin.com](https://edu.zhaozimin.com)，.cn 在大陆、.com 在海外，哪边快用哪边。）

# 欢迎回家

这里是你的笔记库。

现在它还是空的，但 Dataview、Minimal 主题、配色和 CSS 已经布置好了。跟着下面三步，一分钟就能住进来——开荒完成时，项目、人脉、复盘三套系统会一起长出来。

---

## 三步开荒

**第一步**：Obsidian 打开这个文件夹的时候，会弹出一个窗口问你信不信任。点「信任作者并启用插件」。ziminOS、Dataview 和 Style Settings 会一起启用。

**第二步**：点左下角的齿轮图标，打开设置。在左边那一列往下找，找到 **ziminOS**，点它。

进去以后，顶上有一排标签：**开荒、项目、灵感、复盘、人脉、客户、外观、边栏**。一个标签就是一套系统，各管各的设置，你现在停在第一张「开荒」上。

**第三步**：点「初始化」按钮。

它会问你叫什么名字。填上，回车。不想填就按 Esc 跳过。

屏幕右上角冒出「开荒完成 ✅」，就成了。

这时候会自动打开这份 README——也就是你现在看的这一篇，往下翻就是日常用法。每天办事的入口是 `90-system/导航`，把它钉在标签页上，以后每天都从它开始。

---

## 先看一眼最左边那一列

开荒完成后，屏幕最左边那一条竖着的图标栏里，会多出七个 ziminOS 的图标。**鼠标停上去会告诉你它是谁，点一下就直接执行**，不用记快捷键、也不用打字：

| 图标 | 它做什么 |
| --- | --- |
| 一面旗 | 新建项目 |
| 一个灯泡 | 记录灵感 |
| 一本日历 | 打开今天的日记 |
| 一支笔 | 写复盘主题 |
| 一个人带颗心 | 新建人脉 |
| 一份礼物 | 记人情 |
| 一块调色盘 | 打开外观开关 |

这七个是默认摆出来的，判据是「一天里可能按不止一次」。ziminOS 一共有二十六条命令，剩下十九条都在**设置 → ziminOS → 边栏**那一页里，勾一下就出现在这一列，取消勾选就收走。

下面每个动作我都会写「按 `Cmd + P`」那条路——因为二十六条命令都能那么走，而边栏上只摆着你勾过的那几个。两条路通向同一件事，用顺手的那条。

---

## 日常八个动作

### 一、开一个新项目

按 `Cmd + P`（Windows 是 `Ctrl + P`），输入「新建项目」，回车。

它问你三件事：项目叫什么、这个项目跟谁有关、你想把它做成什么样。

跟谁有关那一步有三个选项：**自己做**（只有你，选完直接进下一步）、**和别人一起做**（选一个人，记成同行者）、**客户委托的**（选一个人，记成客户——写下它就等于宣告「我欠他一个交付」）。选了自己做就不会再问你人。

填完，一个项目文件夹就建好了，里面躺着一张项目总览页。

### 一之二、开一个新领域

按 `Cmd + P`，输入「新建领域」，回车。

它只问两件事：领域叫什么、这个领域是什么。填完，`02-areas` 里就多了一个文件夹和一篇总览。

**项目和领域差在哪？看它有没有终点。**「把课讲成一本书」有终点，那是项目；「健康」「手艺」「人脉」没有终点，那是领域。领域不问「这是谁的」——它天然是你自己的。

### 二、写一张卡片

每个项目和领域的文件夹里，都有一篇 `MOC-` 开头的笔记，那是这个项目的总览页——文件名前面那三个字母就是给你认的，它在文件树里永远排在最上面。

在项目总览页里，连打两个左方括号 `[[`，输入卡片的名字，回车。

现在页面上多了一个蓝色的链接。点它。

一张新卡片就长出来了。它会问你「这张卡片讲什么」，用一句话回答就行。

时间、编号、归属，它自己填，你不用管。

### 三、把项目收起来

项目做完了，按 `Cmd + P`，输入「完成项目」，回车。

整个项目文件夹会自己搬进「已归档」，状态改成已完成。

不想做了，用「放弃项目」。只是先放放，用「暂停项目」。

哪天想捡回来，用「重新开始项目」，它会原样搬回来。

### 四、随手记一条灵感

按 `Cmd + P`（Windows 是 `Ctrl + P`），输入「记录灵感」，回车。

把脑子里刚冒出来的那句话写进去，再回车。它会自动进入 `00-inbox/灵感集.md`，最新一条排在最前面，还会带上今天的日期和时间。灵感集第一行先显示标题，Dataview 从第二行开始展示未完成任务，可以直接勾选；任务组标题会显示这篇灵感集的最后更新时间，下方保留全部历史记录。

想换文件夹、笔记名称、插入位置或记录格式，就到设置 → ziminOS → 灵感那一页改。想真正一键呼出，可以在设置 → 快捷键里搜索「记录灵感」，绑定自己顺手的按键。

### 五、读一本书，把划线搬进来

读书这件事在 ziminOS 里是三步：**建一本书 → 把划线导进来 → 把值得展开的那几条摘成卡片**。

**第一步，建书。** 按 `Cmd + P`，输入「新建读书笔记」，回车。

它问三件事：书叫什么、作者是谁、为什么想读。后两个不想填就按 Esc 跳过。

填完，`01-projects` 里多了一个《书名》文件夹，里面是这本书的总览页。**一本书就是一个项目**——它有终点（读完），所以读完之后用的还是那条「完成项目」，整个文件夹搬进已归档。

想补出版社、豆瓣评分这类信息，写进总览页的「书籍信息」小节就行。装了豆瓣插件的话，也可以让它往这里插一段——装不装都不影响别的。

**第二步，导划线。** 你在微信读书、Kindle、苹果图书里划的线，都能搬进来，路子是同一条：在那边导出成文字，回到这边粘贴。

| 你用的 | 怎么导出来 | 能导进来什么 |
| --- | --- | --- |
| 微信读书 | 我 → 笔记 → 选这本书 → 右上角分享 → 复制到剪贴板（手机上文字多了会被截断，建议用电脑端） | 划线、章节、你写的想法 |
| Kindle | 用数据线连电脑，把设备里的 `documents/My Clippings.txt` 打开，全选复制 | 划线、你在设备上写的笔记（Kindle 不给章节） |
| 苹果图书 | 选中划线的句子 → 拷贝（多段就多复制几次，一起粘贴进来也认） | 划线（苹果的拷贝里不带章节，也不带你写的批注） |

最后那一列不是遗漏，是那三个 App 导出来的东西本来就不一样多——Kindle 和苹果图书的划线导进来是平铺的一串，没有章节标题，这是正常的。

复制好之后，打开这本书的总览页，按 `Cmd + P`，输入「导入读书划线」，回车。把刚才复制的东西粘进去，`Cmd + Enter` 提交。

它会告诉你**这段文字是从哪来的、认出的是哪本书、要写进哪本书、新增几条、跳过几条**，你点确认才会写进去。要是它认出来的书名和你要写进去的那本对不上，确认框上会亮一条橙色警告——**看到那条就先别点确认**，多半是你站错了书。

四件事值得先知道：

- **可以反复导。** 读一半导一次，读完再导一次，已经在里面的不会重复——它认的是句子本身，你之前把某条划线加粗过也照样认得出。
- **它只往里加，不改你写的东西。** 你在划线下面加的批注、改的格式，一个字都不会动。反过来说，你删掉的划线下次导入会回来。
- **后来才写的想法也补得进去。** 你上周导过的划线，这周在微信读书里给它写了想法，再导一次，想法会挂到那条划线下面。
- **Kindle 那个文件里是你所有的书。** 粘进来之后它会让你选一本，只导你选的那本。

**第三步，摘成卡片。** 划线是书的，卡片才是你的。

在总览页里选中一条值得展开的划线，按 `Cmd + P`，输入「摘成卡片」，回车。给它起个名字；接着问「这张卡片讲什么」，想写就写，不想写按 Esc 跳过。

（如果它说「现在是阅读模式」，按 `Cmd + E` 切回编辑模式再选一次——阅读模式下你选中的字插件读不到。）

书的文件夹里就长出一张卡片：开头是那条划线的原文（引用块），下面是空的——那是留给你写「所以呢」的地方。卡片会自动出现在总览页底部那张表里。

**一本书读完的标志不是划线多，是摘出了几张卡片。** 划线只是原料。

---

## 每天一句话，一年一条主线

### 六、写今天的日记

按 `Cmd + P`，输入「打开今天的日记」，回车。今天那一篇就出来了，没有的话它自己建。

日记只写两样东西：上半截「今天做了什么」一行一件事，写过程；下半截「今日产出」自动列出你今天新建和改动了哪些笔记，不用管。

**傍晚收工前，再按一次 `Cmd + P`，输入「写复盘主题」**，用一句话说清今天主要做了什么。就这一句。

这一句是整套复盘的入口。你只写这一次，剩下的四级全是它的投影：

- 周复盘看七句日主题连起来是什么
- 月复盘看四条周主题连起来是什么
- 一直到年

「打开本周复盘 / 本月 / 本季 / 本年」也都在命令面板里。站在周记上按「写复盘主题」，写的就是周主题——一条命令管五级，它自己认得出你站在哪。

复盘笔记本身是干净的：一个手写小节，加几张自动长好的表，没有一句提示文字。打开它，对着自动表回答下面的问题，一问一段，写完就算复盘完：

- **周复盘**：七天的主题连起来，我这周真正在推进的是什么？哪一天偏离了主线，为什么？下周只改一件事，改什么？
- **月复盘**（看结果，不看过程）：几条周主题连起来，这个月真正完成的是什么？完成的项目里哪一个最值得复盘？下月唯一优先级是什么，为它砍掉什么？
- **季复盘**（看系统，不看事件）：三个月的主线是在推进还是在漂移？完成的项目里有多少是季初就打算做的？下季度做什么、坚决不做什么？
- **年复盘**：十二个月的主题连起来，今年的主线是什么？三大得、三大失各是哪三件，教训各一句？明年主题定什么，它要求我成为什么样的人？

这几个问题不用背，忘了就回这里看一眼。

### 七、把人记下来

按 `Cmd + P`，输入「新建人脉」。它问三件事：他叫什么、多久该跟他说句话（密/近/熟/远）、这段关系往哪个方向使劲。

建好之后，**你再也不用打开这份档案去更新它**。日常发生的事只写一处——当天日记，句子里带上 `[[他的名字]]`。三种写法自动分流到他的档案：

| 你在日记里写 | 它落到哪 |
| --- | --- |
| `- 和 [[张三]] 去了新疆，谈定一起投一家网咖` | 他档案的「关键事件」 |
| `- [ ] 出网咖投资方案给 [[张三]]` | 他档案的「待办」 |
| `- [[张三]]｜去｜送了半斤生普｜两清` | 他档案的「人情账本」 |

最后那种也可以用命令「记人情」四步点选，不用手打。

打开 `02-areas/人脉/人脉MOC`，四张表自动长好：谁在名录里、该给谁送东西、这个月谁过生日、谁还欠着谁。**「最近联系」是从日记里算出来的，不用你填**——超过该层节奏的会自己标上 ⚠️，那就是该找他的信号。

### 靠卖东西吃饭的，再加一步

按 `Cmd + P`，输入「初始化客户模块」，跑一次。客户 MOC、模板和目录一次长齐，多出三条命令：「新建客户」「增加付费」「记收款」。

不做生意就别跑它——没跑之前，你的库里不会多出任何一张空表。

收了一笔钱，打开那位客户的档案，按 `Cmd + P`，输入「增加付费」，点选产品、金额、日期就写进去了，不用手打格式。写进去的是「付费与交付」小节里的一行任务：**没打勾＝这笔的东西还欠着，交付完了就点一下勾**。想手写或想弄懂格式，客户 MOC 底部的「使用说明」里有完整示例。

---

## 七个文件夹，各管一摊

| 文件夹 | 放什么 |
| --- | --- |
| `00-inbox` | 收件箱。脑子里冒出来的东西先扔这儿，以后再说。 |
| `01-projects` | 项目。有明确结果、你希望它结束的事。你在读的书也住这儿——读完就是它的终点。 |
| `02-areas` | 领域。要长期照顾、没有终点的事。比如健康、家人、手艺、人脉。 |
| `03-resources` | 资料。你感兴趣的主题，收藏的、查来的东西。 |
| `04-archives` | 已归档。做完的、放弃的、暂停的，都在这儿躺着。 |
| `05-diary` | 日记与复盘。日、周、月、季、年五层，时间轴住这儿。 |
| `90-system` | 系统。导航页和模板住在这里。平时不用进来。 |

前五个加最后一个回答「这东西属于谁」，`05-diary` 回答「这事什么时候发生」。两种问法不是一回事，所以它单独占一格。

一句话记住顺序：**先收进来，再动起来，长期养着，随手查着，做完收起来，天天记着，工具放一边。**

---

## 四个小提醒

**关于外观**：笔记库默认使用 Minimal 主题和 ziminOS 配色。你可以在设置 → Style Settings 里改颜色；升级 ziminOS 不会把你改过的配色覆盖掉。

看右下角，有个 🎨 按钮。点开是十二个外观片段的开关——文件夹前面的图标、彩虹引导线、代码块行号、当前行高亮、属性栏自动收起，都在这里，看着不顺眼随手关掉，立刻生效，不用重启。有两个默认是关着的（代码块横向滚动、水平线中间加图标），想试就打开。

这些开关记的是你的选择，升级 ziminOS 不会替你重新打开你关掉的东西。嫌这个按钮碍眼，可以在设置 → ziminOS → 外观那一页把它收起来——最左边那列的调色盘图标和命令面板里的「打开外观开关」照样能打开它，三条路通向同一个面板。

**关于左边那列图标**：想加想减都在设置 → ziminOS → 边栏那一页，二十六条命令列在那儿，每行左边就是它在边栏上的样子。两件事先说在前面：其一，取消勾选后它在「设置 → 外观 → 功能区」和手机端的边栏菜单里要重启 Obsidian 才彻底消失；其二，反过来，你在 Obsidian 自己那个「功能区」里藏掉的图标，回 ziminOS 这边勾上也不会出现——那是 Obsidian 的开关，得回那儿打开。

**关于排版**：你写完一篇笔记、切到别的笔记去，ziminOS 会顺手替你把这一篇整理成标准的 Markdown 写法——汉字和英文、数字之间补一个空格，标题、列表、表格、代码块上下各留一个空行，多余的空行收成一行。**它不会在你正打字的那一篇上动手**，所以不用担心打到一半被打断；想当场整理就按 `Cmd + P`，输入「整理当前笔记格式」。

有一件事它绝对不做：动你的双链。`[[人脉MOC]]` 里面一个空格都不会加，空格只补在方括号外面——链接照样点得动。

九条规则都能单独关掉，路径是设置 → ziminOS → 排版。不喜欢中文里插空格的，就关掉第一条，其余八条照常。

**关于模板**：如果你想手动套模板，需要打开 Obsidian 的「模板」功能。路径是：设置 → 核心插件 → 找到「模板」→ 打开开关。大多数时候它本来就是开的，你可以先不管这条。

**关于图片**：往笔记里贴图片的时候，旁边会自动冒出一个叫「附件」的文件夹，图片都在里面。这是正常的，别删它。

---

## 想学得更系统

插件教程和系统课在最上面那张名片里——回到本页顶部就能看到。同一张名片也在设置 → ziminOS → 开荒页的最底部。
